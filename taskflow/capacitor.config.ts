import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.miracle',
  appName: 'Miracle TaskFlow',
  webDir: 'dist'
};

export default config;

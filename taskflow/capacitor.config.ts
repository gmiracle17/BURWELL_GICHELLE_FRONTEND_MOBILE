import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.miracle',
  appName: 'miracletaskflow',
  webDir: 'dist'
};

export default config;
